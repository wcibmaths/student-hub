kind = "web"
previewPath = "/"
title = "Wellington Maths Hub"
version = "1.0.0"
id = "artifacts/maths-hub"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22620

[services.development]
run = "pnpm --filter @workspace/maths-hub run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/maths-hub", "run", "build" ]
publicDir = "artifacts/maths-hub/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22620"
BASE_PATH = "/"
